# ToS スケジュール管理 Web アプリケーション 仕様書

## 1. 概要

### 1.1 目的
ToS（Total System）開発プロジェクトのスケジュールは現在 draw.io ファイルで管理されているが、都度 XML を編集して更新するのは非効率である。ブラウザからアクセスできるWebアプリケーションとして、スケジュールバーの移動・リサイズ、マイルストンの追加・編集・削除を直感的に操作でき、画像（PNG/PDF）でエクスポートして開発メンバーに共有できるシステムを構築する。

### 1.2 スコープ
- ローカル完結（localhost）での個人利用
- 既存 draw.io ファイルからのデータインポート
- ガントチャート形式のスケジュール表示・編集
- PNG / PDF エクスポート
- JSON ファイルによるデータ永続化（Git管理可能）

## 2. 機能要件

### 2.1 表示機能
| ID | 機能 | 説明 |
|----|------|------|
| F-01 | ガントチャート表示 | SVGベースでdraw.ioレイアウトを忠実に再現 |
| F-02 | タイムラインヘッダ | 年 / 四半期(Q) / 月 の3層ヘッダ表示 |
| F-03 | スイムレーン表示 | ラベル（140px固定幅）付きの行区切り |
| F-04 | スケジュールバー | 角丸矩形、8色カラーマップ、ラベルテキスト |
| F-05 | マイルストン | ★マーク（赤、太字）で表示 |
| F-06 | 本日線 | 現在日付を示す赤い縦線 |
| F-07 | ページ切替 | 4ページ（タブUI）の切替 |

### 2.2 編集機能
| ID | 機能 | 説明 |
|----|------|------|
| F-10 | バーのドラッグ移動 | 月単位スナップでバーを水平移動 |
| F-11 | バーのリサイズ | 左右端ハンドルで期間変更（月単位スナップ） |
| F-12 | マイルストンのドラッグ | 月単位スナップで移動 |
| F-13 | ダブルクリック編集 | バー→BarEditor、マイルストン→MilestoneEditor ダイアログ |
| F-14 | 新規追加 | バー追加ボタン、マイルストン追加ボタン |
| F-15 | 右クリックメニュー | 削除、複製 |
| F-16 | Undo/Redo | Ctrl+Z / Ctrl+Y（スナップショット方式） |

### 2.3 データ管理
| ID | 機能 | 説明 |
|----|------|------|
| F-20 | 自動保存 | 変更から5秒後に自動保存 |
| F-21 | 手動保存 | ツールバーの保存ボタン |
| F-22 | JSONファイル保存 | `data/schedule.json` への読み書き |
| F-23 | draw.ioインポート | draw.io XMLファイルからJSON形式への変換 |

### 2.4 エクスポート
| ID | 機能 | 説明 |
|----|------|------|
| F-30 | PNGエクスポート | Puppeteerによる高品質PNG生成 |
| F-31 | PDFエクスポート | A3横レイアウト、游ゴシックフォント |

## 3. 技術スタック

| レイヤー | 技術 | バージョン | 選定理由 |
|---------|------|-----------|---------|
| Frontend | Vite + React + TypeScript | Vite 6, React 19, TS 5 | 高速ビルド、型安全 |
| 描画 | SVG (React コンポーネント) | - | draw.ioレイアウト忠実再現、ベクター品質 |
| 状態管理 | Zustand + Immer | 最新 | 軽量、Undo/Redo容易 |
| Backend | Express.js | 最新 | 最小API（JSONファイルR/W + エクスポート） |
| エクスポート | Puppeteer | 最新 | 高品質PNG/PDF生成 |
| データ | JSONファイル | - | DB不要、Git差分管理可能 |

## 4. データモデル

### 4.1 全体構造 (ScheduleData)
```typescript
interface ScheduleData {
  version: string;                // "1.0.0"
  lastModified: string;           // ISO 8601
  timeline: {
    startDate: string;            // "2025-10" (ページ0の開始)
    endDate: string;              // "2028-08"
    monthWidthPx: number;         // 55 (ページ0), 42 (ページ1)
    laneHeaderWidthPx: number;    // 140
  };
  pages: SchedulePage[];
}
```

### 4.2 ページ (SchedulePage)
```typescript
interface SchedulePage {
  id: string;                     // "p0", "p1", "p2", "p3"
  name: string;                   // "ToS開発計画"
  timeline?: {                    // ページ固有のタイムライン設定（省略時は全体設定を継承）
    startDate: string;
    endDate: string;
    monthWidthPx: number;
  };
  swimLanes: SwimLane[];
  annotations: Annotation[];
}
```

### 4.3 スイムレーン (SwimLane)
```typescript
interface SwimLane {
  id: string;
  label: string;                  // "Total\nSystem"
  heightPx: number;
  bars: ScheduleBar[];
  milestones: Milestone[];
}
```

### 4.4 スケジュールバー (ScheduleBar)
```typescript
interface ScheduleBar {
  id: string;
  label: string;
  startMonth: string;             // "2026-04"
  endMonth: string;               // "2026-09"
  color: "blue"|"pink"|"green"|"orange"|"gray"|"purple"|"red"|"security";
  yOffsetInLane: number;
  heightPx: number;
  style?: { dashed?: boolean; opacity?: number };
}
```

### 4.5 マイルストン (Milestone)
```typescript
interface Milestone {
  id: string;
  label: string;
  date: string;                   // "2026-07"
  yOffsetInLane: number;
}
```

### 4.6 注釈 (Annotation)
```typescript
interface Annotation {
  id: string;
  type: "note" | "copyright";
  text: string;
  x: number;
  y: number;
  width: number;
  height: number;
}
```

## 5. カラーマッピング

| 名前 | 用途 | fill | stroke | fontColor |
|------|------|------|--------|-----------|
| blue | 結合テスト | #dae8fc | #6c8ebf | #333333 |
| pink | FOT/異常系 | #f8cecc | #b85450 | #333333 |
| green | オペレーション | #d5e8d4 | #82b366 | #333333 |
| orange | 開発 | #fff2cc | #d6b656 | #333333 |
| gray | 仕様 | #f5f5f5 | #999999 | #333333 |
| purple | 許認可 | #e1d5e7 | #9673a6 | #333333 |
| red | マイルストンバー | #e53935 | #c62828 | #ffffff |
| security | セキュリティ検証 | #ffcdd2 | #e53935 | #333333 |

## 6. 画面レイアウト

### 6.1 全体構成
```
┌─────────────────────────────────────────────┐
│ Toolbar: [保存] [Undo] [Redo] [+バー] [+★]  │
│          [PNG] [PDF]                         │
├─────────────────────────────────────────────┤
│ PageTabs: [ToS開発計画] [中長期] [BMS] [RMS] │
├────────┬────────────────────────────────────┤
│ レーン  │ タイムライン（年/Q/月 3層ヘッダ）    │
│ ヘッダ  ├────────────────────────────────────┤
│ (140px) │ スイムレーン + バー + マイルストン    │
│        │ （横スクロール対応）                  │
│        │                                    │
│        │         ← 本日線 →                  │
│        │                                    │
└────────┴────────────────────────────────────┘
```

### 6.2 ツールバー
- 保存ボタン（💾）
- Undo / Redo ボタン
- バー追加ボタン（＋バー）
- マイルストン追加ボタン（＋★）
- PNGエクスポートボタン
- PDFエクスポートボタン

### 6.3 編集ダイアログ

#### BarEditor（バー編集）
- ラベル（テキスト入力）
- 開始月（YYYY-MM セレクタ）
- 終了月（YYYY-MM セレクタ）
- カラー（8色から選択）
- 削除ボタン

#### MilestoneEditor（マイルストン編集）
- ラベル（テキスト入力）
- 日付（YYYY-MM セレクタ）
- 削除ボタン

## 7. API 仕様

### 7.1 スケジュールデータ取得
```
GET /api/schedule
Response: ScheduleData (JSON)
```

### 7.2 スケジュールデータ保存
```
PUT /api/schedule
Body: ScheduleData (JSON)
Response: { success: true, lastModified: string }
```

### 7.3 PNGエクスポート
```
POST /api/export/png
Body: { pageId: string }
Response: image/png (binary)
```

### 7.4 PDFエクスポート
```
POST /api/export/pdf
Body: { pageId: string }
Response: application/pdf (binary)
```

## 8. ディレクトリ構造

```
project-management-tool/
├── 仕様書.md                      # 本ドキュメント
├── Tosスケジュール.drawio          # 原本（読み取り専用）
├── Tosスケジュール.pdf
└── webapp/
    ├── package.json
    ├── vite.config.ts
    ├── tsconfig.json
    ├── tsconfig.node.json
    ├── data/
    │   └── schedule.json            # メインデータファイル
    ├── src/
    │   ├── main.tsx                 # エントリポイント
    │   ├── App.tsx
    │   ├── App.css
    │   ├── types/
    │   │   └── schedule.ts          # 型定義
    │   ├── components/
    │   │   ├── GanttChart/
    │   │   │   ├── GanttChart.tsx         # メインSVGコンテナ
    │   │   │   ├── TimelineHeader.tsx     # 年/Q/月 3層ヘッダ
    │   │   │   ├── SwimLane.tsx           # スイムレーン行
    │   │   │   ├── ScheduleBar.tsx        # ドラッグ可能バー
    │   │   │   ├── Milestone.tsx          # ★マイルストン
    │   │   │   └── TodayLine.tsx          # 本日線
    │   │   ├── PageTabs.tsx               # ページ切替タブ
    │   │   ├── Toolbar.tsx                # 保存/エクスポート/Undo
    │   │   ├── BarEditor.tsx              # バー編集ダイアログ
    │   │   └── MilestoneEditor.tsx        # マイルストン編集ダイアログ
    │   ├── hooks/
    │   │   ├── useDragResize.ts           # ドラッグ&リサイズ
    │   │   ├── useUndoRedo.ts             # Undo/Redo
    │   │   └── useScheduleStore.ts        # Zustandストア
    │   └── lib/
    │       ├── date-utils.ts              # 日付⇔ピクセル変換
    │       ├── color-map.ts               # バーカラー定義
    │       ├── drawio-importer.ts         # draw.io XMLパーサー
    │       └── constants.ts               # 定数
    ├── server/
    │   ├── index.ts                       # Express サーバー
    │   └── routes/
    │       ├── schedule.ts                # JSON CRUD API
    │       └── export.ts                  # PNG/PDF生成
    └── scripts/
        └── import-drawio.ts               # draw.io→JSON変換CLI
```

## 9. draw.io ファイル構造

### 9.1 ページ構成
| ページID | ページ名 | タイムライン範囲 | 月幅(px) |
|---------|---------|----------------|---------|
| p0 | ToS開発計画 | 2025-10 〜 2027-08 | 55 |
| p1 | 中長期スケジュール | 2026-01 〜 2028-03 | 42 |
| p2 | BMS詳細 | (要確認) | (要確認) |
| p3 | RMS詳細 | (要確認) | (要確認) |

### 9.2 スイムレーン（ページ0: ToS開発計画）
| レーン | ラベル | Y範囲 |
|--------|--------|-------|
| Milestone | Milestone | 94-140 |
| Total System | Total System | 140-270 |
| TMS (NEC) | -運行管理 TMS (NEC) | 270-365 |
| BMS (内製) | -拠点管理 BMS (内製) | 365-480 |
| 車両動態 | -車両動態 (T.B.D) | 480-510 |
| ADS | ADS | 510-575 |
| RMS | 遠隔監視 RMS | 575-690 |

### 9.3 セル分類ルール
- **ヘッダセル**: y < 94, fillColor=#e3f2fd or #bbdefb
- **レーンラベル**: x=0, width=140
- **バー**: rounded=1, 各種fillColor
- **マイルストン**: text style, ★で始まるvalue, fontColor=#d32f2f
- **罫線**: endArrow=none (edge要素)
- **注釈**: 大きなテキストボックス (y > 690)

## 10. 操作フロー

### 10.1 初回セットアップ
1. `npm install` — 依存パッケージインストール
2. `npm run import` — draw.ioファイルからJSONデータ生成
3. `npm run dev` — 開発サーバー起動（frontend + backend）
4. ブラウザで `http://localhost:5173` にアクセス

### 10.2 日常利用
1. `npm run dev` でサーバー起動
2. ブラウザでスケジュール表示
3. バーのドラッグ / リサイズ / ダブルクリック編集
4. マイルストンの追加 / 編集 / 削除
5. 自動保存（5秒後）または手動保存
6. PNG/PDF エクスポートで共有

## 11. 非機能要件

| 項目 | 要件 |
|------|------|
| デプロイ | ローカル完結（localhost） |
| ユーザー数 | 個人利用（1名） |
| ブラウザ | Chrome最新版 |
| レスポンス | 描画: 1秒以内、保存: 即時 |
| データ量 | 4ページ、各ページ最大100要素程度 |

## 12. 検証方法

1. **インポート検証**: `npm run import` → `data/schedule.json` が生成され、4ページ分のデータが含まれること
2. **描画検証**: `npm run dev` → ブラウザで開き、draw.ioファイルと同等のレイアウトが表示されること（目視確認）
3. **操作検証**: バーをドラッグ→月単位でスナップ、マイルストンをダブルクリック→編集ダイアログ表示
4. **保存検証**: 変更後に `data/schedule.json` が更新されていること
5. **エクスポート検証**: PNG/PDFボタン→ファイルがダウンロードされ、内容が画面表示と一致
6. **Undo/Redo検証**: Ctrl+Z で直前の操作が取り消されること
